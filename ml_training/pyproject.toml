[project]
name = "ml-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2.2",
    "pillow>=12.1.0",
    "python-dotenv>=1.2.1",
    "torch>=2.9.0",
    "huggingface-hub==0.34.6",
    "transformers>=4.48.0",
    "hf-xet>=1.2.0",
    "scikit-learn>=1.6.1",
    "evaluate>=0.4.6",
    "datasets>=3.6.0",
    "pyarrow>=19.0.0",
    "jiwer>=3.1.0",
    "matplotlib>=3.10.0",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ml_training"]

[project.scripts]
train = "ml_training.main:main"
